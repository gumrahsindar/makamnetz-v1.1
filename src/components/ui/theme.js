import { createTheme } from '@mantine/core'

export const theme = createTheme({
  // add three font families
  fontFamily: 'Inter, sans-serif',
  fontFamilyMonospace: 'Monaco, Courier, monospace',
  headings: {
    fontFamily: 'Inder, sans-serif',
  },
})
